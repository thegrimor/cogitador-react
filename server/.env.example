DATABASE_URL="postgresql://cogitador:cogitador@localhost:5432/cogitador?schema=public"
JWT_SECRET="change-me-to-a-long-random-string"
JWT_EXPIRES_IN="7d"
PORT=4000
CORS_ORIGIN="http://localhost:5173"

# Password reset tokens are generated and stored, pero el envío real de email
# no está implementado (no hay proveedor SMTP/API configurado). Ver README.
